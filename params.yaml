# Chon mot trong: random_forest, extra_trees, gradient_boosting, logistic_regression
model_type: extra_trees
n_estimators: 800
max_depth:
min_samples_split: 2
n_jobs: -1

# Tham so bonus cho cac thuat toan khac.
learning_rate: 0.1
C: 1.0
max_iter: 1000
